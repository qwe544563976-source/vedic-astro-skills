# 本仓会话家规

## 主窗口分工铁律(2026-07-19 盘主定,强制)
1. **主窗口只干四件事:对齐需求 / 判断拍板 / 写派工单 / 验收回执+答问。** 不亲手改文件、不亲手挖数据、不亲手跑代码。
2. 一切动手活(审计/改码/查包/写文件/记账)一律派子任务,**默认用 sonnet(claude-sonnet-5)**;主窗口的 token 只花在判断上。
3. 派工单必须带四样:范围、只读还是可写、验收标准、诚实条款("查不出就说查不出,不许硬凑发现")。
4. 子任务回执后,主窗口只验收+判断,不重复干活;回执对不上验收标准=退回返工,不迁就。
